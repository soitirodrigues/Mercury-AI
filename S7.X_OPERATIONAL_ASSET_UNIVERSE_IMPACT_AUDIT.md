# S7.X — OPERATIONAL ASSET UNIVERSE IMPACT AUDIT

**Data:** 2026-09-08 · **Branch:** `main` · **Workspace:** `C:\Projetos\Mercury-AI`
**Modo:** SOMENTE LEITURA — nenhuma alteração de produção, teste ou configuração foi realizada.
**Equipe:** Auditoria forense automatizada (5 sub-agentes paralelos + verificação manual)
**Regra Zero:** Inteligência certificada protegida — nenhum motor/fórmula/peso/threshold foi tocado.

---

## 1. EXECUTIVE SUMMARY

O Mercury AI possui **fonte oficial declarada** única — `mercury_ai/config/universe.py` (64 ativos: 28 FOREX + 10 CRIPTO + 23 STOCK + 3 COMMODITY, L1-476) — mas **fonte efetiva em produção** é a interseção de dois caches materializados em disco: `data/asset_registry.json` (62) ∩ `data/brokers/XP.json` (62). A divergência é de **2 ativos órfãos** (`EURNZD=X`, `POL-USD`) presentes no código e no `YahooAdapter.supported_assets` mas nunca escaneados porque `scripts/sync_broker_registry.py` filtra pelo `distinct PASS` de `reports/asset_universe/hezilex_asset_mapping.json` (62).

O `MercuryScanner.scan()` (`mercury_ai/brain/scanner.py:69`) **não aceita lista externa** — universo é reconstruído internamente via `MercuryConfigCenter` (`config.json` `OPERATIONAL_PROFILE` → `XP`) + `AssetRegistry` (`get_assets_for_broker` L115-123 + filtro `enabled/profile` L104-112). Reduzir de 62 → 40 (ou 27+13 do universo operacional desejado) é **arquiteturalmente direto** e **suportado nativamente** pelo `AssetRegistry` (flags `enabled`, `profile`, `get_enabled_assets` L111, `filter_assets` L130, `set_enabled` L92), mas exige mutação de **2 arquivos JSON em disco** ou do `universe.py` + re-sync — não há API de parâmetro `symbols` no scanner.

`BTC/USD` já é CRYPTO arquiteturalmente (`BTC-USD`, `market="CRYPTO"` em `universe.py:261` e `market="Cripto"` em `data/asset_registry.json`) — sem gap de mapeamento Yahoo (`BTC-USD` via `yf.download`).

Market data **não converte** símbolo (`BTC/USD` com `/` → `ValueError` em `providers/base_provider.py:14 sanitize_symbol` e `providers/yahoo_finance_provider.py:22`); normalização é agnóstica (`data/data_normalizer.py:38 lowercase + L68 duplica `Open/open``).

**Gap crítico:** `mercury_ai/sessions/market_sessions.py:1-23` é stub puro (só `datetime.utcnow().hour` sem `weekday`/`weekend`/`is_market_open(symbol, dt)`). Mercury **não sabe** quando Forex está fechado, não diferencia Forex 24/5 de Crypto 24/7, e escaneia 27 pares Forex inutilmente todo fim de semana (`M5Clock` `operations/m5_operational/clock.py:19` + `M5OperationalRunner` `operations/m5_operational/runner.py:140` sem gate de calendário). Impacto é operacional/telemetria, não inteligência.

Replay (`HistoricalReplayEngine` L71, `HistoricalReplayProvider` L15, `ReplayCache` L38, `ReplayBatchProcessor` L83) é **universo-agnóstico** (parametrizado por `symbol, DataFrame, run_id`, 0 imports de `AssetRegistry/XP.json`) — redução só diminui candidatos/ranking, sem bug de look-ahead/cache/clock.

Testes: **0 asserts de contagem fixa** (`len==62/63`); ~103/115 não afetados (mocks/sintéticos); **2 afetados** (`tests/test_broker_filtering.py:25` sobrescreve `XP.json` real, `tests/test_scanner_recovery.py:16` mocka autorizados em 2) mas não quebram com universo reduzido; 6 docs/fixtures mereceriam atualização cosmética.

**Veredito:** `AUDIT COMPLETE — IMPLEMENTATION NOT YET AUTHORIZED`. Arquitetura **suporta** universo restrito de 40 via configuração existente; menor alteração futura é editar `data/brokers/XP.json` + desabilitar em `data/asset_registry.json` (ou alinhar `universe.py` → re-sync) e introduzir `is_market_open()` em `market_sessions.py` + gate em `runner.py`. Nenhum motor protegido precisa ser tocado.

---

## 2. CURRENT ASSET UNIVERSE

Evidência medida via `python -c json.load` + `from mercury_ai.config.universe import ALL_SYMBOLS`:

| Fonte | Total | Breakdown | Evidência |
|-------|-------|-----------|-----------|
| `mercury_ai/config/universe.py` `ALL_SYMBOLS` L415-417 | **64** | FOREX 28, CRYPTO 10, STOCK 23, COMMODITY 3 | `len(ALL_SYMBOLS)==64`, `FOREX_UNIVERSE` 28 L40-192, `CRYPTO_UNIVERSE` 10 L194-253, `STOCK_UNIVERSE` 23 L273-390, `COMMODITY_UNIVERSE` 3 L395-420 |
| `data/asset_registry.json` | **62** | Forex 27, Cripto 9, Stocks 23, Commodities 3 | `Counter(category)` → `{'Forex':27,'Stocks':23,'Cripto':9,'Commodities':3}`, 62 chaves sorted list provada |
| `data/brokers/XP.json` | **62** | idem | `len==62`, `set(registry)-set(XP)==∅`, `set(XP)-set(registry)==∅` |
| `reports/asset_universe/hezilex_asset_mapping.json` | 68 entries | 63 PASS (62 distintos), 5 UNSUPPORTED | distinto PASS 62 = `registry==XP` |
| `mercury_ai/providers/data_adapters.py` `YahooAdapter.supported_assets` L54 | 64 | `ALL_SYMBOLS` | `supported_assets=ALL_SYMBOLS`, `is_implemented=True` L58 |

Símbolos em disco (62, ordenados):
```
AAPL, AMZN, AUDCAD=X, AUDCHF=X, AUDJPY=X, AUDNZD=X, AUDUSD=X, AVAX-USD, BAC,
BNB-USD, BTC-USD, C, CADCHF=X, CADJPY=X, CHFJPY=X, CL=F, DIS, DOGE-USD, ETH-USD,
EURAUD=X, EURCAD=X, EURCHF=X, EURGBP=X, EURJPY=X, EURUSD=X, F, GBPAUD=X, GBPCAD=X,
GBPCHF=X, GBPJPY=X, GBPNZD=X, GBPUSD=X, GC=F, IBM, INTC, JPM, KO, MA, MCD, META,
MSFT, NFLX, NKE, NVDA, NZDCAD=X, NZDCHF=X, NZDJPY=X, NZDUSD=X, PYPL, SBUX, SI=F,
SOL-USD, SPOT, SUI-USD, TSLA, USDCAD=X, USDCHF=X, USDJPY=X, V, WFC, XLM-USD, XRP-USD
```

Divergência: `set(ALL_SYMBOLS) - set(registry) = ['EURNZD=X','POL-USD']` (2 órfãos). Comentários `universe.py` L4-6 citam `27 FOREX +10 =37` e `Total:63` — stale (real 28+10+23+3=64).

---

## 3. OFFICIAL ASSET SOURCE

**Declarada:** `mercury_ai/config/universe.py` L1-35 header: *"Fonte única da verdade ... NÃO duplique listas ... Todos os consumidores devem importar deste módulo."* Confirmado em `reports/asset_universe/source_of_truth.json` → `{"SOURCE_OF_TRUTH":"mercury_ai/config/universe.py :: OPERATIONAL_UNIVERSE"}`.

| Pergunta | Resposta com prova |
|----------|--------------------|
| Onde o universo é definido? | `mercury_ai/config/universe.py` L40 `FOREX_UNIVERSE`, L194 `CRYPTO_UNIVERSE`, L273 `STOCK_UNIVERSE`, L395 `COMMODITY_UNIVERSE`, L425 `OPERATIONAL_UNIVERSE={**FOREX..}`, L433-437 `ALL_SYMBOLS/FOREX_SYMBOLS/...` |
| Existe única fonte oficial? | **Declarada SIM, efetiva NÃO** — ver §13. Em runtime, `MercuryScanner.scan()` nunca lê `ALL_SYMBOLS` diretamente; lê `data/asset_registry.json` ∩ `data/brokers/XP.json` (scanner.py:89-110) |
| Listas duplicadas? | **SIM — 4 fontes** (§13): `universe.py` (64) + `config/assets.py` `ASSET_REGISTRY` (64, shim compat L32-60) + `data/asset_registry.json` (62) + `data/brokers/XP.json` (62) |
| Símbolos hardcoded? | **SIM, mas só em `universe.py`** (definições `UniverseAsset(symbol=...)`). Varredura `pathlib.Path('mercury_ai').rglob('*.py')` + regex 97 hits → nenhum array literal fora `universe.py/assets.py` consumido em produção |
| JSON/YAML/ENV/Python? | **Python** (`universe.py` 476 linhas), **JSON** (`data/asset_registry.json` 62, `data/brokers/XP.json` 62, `hezilex_asset_mapping.json` 68). **YAML: NÃO COMPROVADO** (0 `*.yaml` relevantes em `mercury_ai/config/`, raiz). **ENV: NÃO COMPROVADO** (nenhum `ASSETS=` env var) |
| Quem consome? | `mercury_ai/brain/scanner.py:5` `AssetRegistry` (produção), `mercury_ai/providers/data_adapters.py:3` `ALL_SYMBOLS/FOREX_SYMBOLS/CRYPTO_SYMBOLS` (YahooAdapter), `scripts/sync_broker_registry.py` (gerador), `operations/m5_operational/runner.py:26` `ALL_SYMBOLS`, `tests/*` via `universe` e mocks |

---

## 4. ASSET REGISTRY AUDIT

**Arquivo:** `mercury_ai/core/asset_registry.py` — 134 linhas

| Elemento | Linhas | Evidência |
|----------|--------|-----------|
| `@dataclass class Asset` | 10-28 | 16 campos: `symbol, category, priority, profile, enabled=True, provider="Yahoo", fallback_provider="Polygon", market="Stocks", timeframe="5m", tick_size, pip_size, trading_session, liquidity, spread, favorite, last_operated, previous_score` |
| `class AssetRegistry` docstring | 31-36 | thread-safe + escrita atômica `tempfile.mkstemp + os.replace + fsync` |
| `__init__(registry_file="data/asset_registry.json")` | 38-42 | `self.assets:Dict[str,Asset]`, `self._lock=threading.Lock()`, `_load_from_file()` |
| `_load_from_file` | 44-54 | `json.load → Asset(**details)`, `except (JSONDecodeError,TypeError): pass` (corrompido=registro limpo silencioso) |
| `_atomic_write` | 60-79 | `json.dumps({s:a.__dict__}, indent=4)`, `mkstemp(prefix=".reg_")+os.replace` |
| `register_asset` | 81-90 | cria `Asset` + `_atomic_write` sob lock |
| `set_enabled` | 92-96 | `if symbol in self.assets: a.enabled=...; _atomic_write()` |
| `set_priority` | 98-102 | idem |
| `update_asset_stats` | 104-109 | `last_operated=time.time(); previous_score=score` |
| `get_enabled_assets() -> List[str]` | 111-113 | `[s for s,a in self.assets.items() if a.enabled]` sob lock — **não usado pelo Scanner canônico** |
| `get_assets_for_broker(broker_name)` | 115-123 | `broker_file=f"data/brokers/{broker_name}.json"` — **SEM lock**, path relativo hardcoded, `if not exists: return []`, `except JSONDecodeError: return []` (falha silenciosa) |
| `search_assets(query)` | 125-128 | `query.lower() in symbol.lower()` sob lock |
| `filter_assets(category)` | 130-134 | `==` exato, sob lock — **não usado pelo Scanner** |
| `save()` | 56-58 | delega a `_atomic_write` sob lock |

**Classificação de ativos:** `Asset.category` (`Forex`/`Cripto`/`Stocks`/`Commodities` — PT) e `Asset.market` (duplica `category`, 62/62 idênticos). Sem `Enum`, validação ou normalização; filtro é `a.category==category` case-sensitive.

**Broker association:** `get_assets_for_broker` lê `data/brokers/{broker}.json` cru (só `XP.json` existe, 62 entries). Sem validação de schema, sem cache.

**Consumidores:** único consumidor canônico em produção é `mercury_ai/brain/scanner.py:63` (`self.asset_registry=AssetRegistry()`).

> **Suporta universo restrito naturalmente? SIM — evidência:** `set_enabled(symbol, False)` L92-96 + `filter_assets` L130 + `get_enabled_assets` L111 + interseção `enabled && profile==active && symbol in authorized_symbols` em `scanner.py:104-110`. Basta desabilitar (`enabled=false`) ou remover do `XP.json`; `enabled_assets` reflete automaticamente sem alterar `AssetRegistry`. `validate_symbol` existe em `universe.py:460` mas é órfã (scanner não chama).

---

## 5. ASSET CONSUMER MAP

### Fluxo REAL verificado via imports em `mercury_ai/brain/scanner.py:4-13`

```
[FONTE DECLARADA]
mercury_ai/config/universe.py (476 linhas)
  FOREX_UNIVERSE 28 + CRYPTO_UNIVERSE 10 + STOCK_UNIVERSE 23 + COMMODITY 3 = 64
  ALL_SYMBOLS / FOREX_SYMBOLS / CRYPTO_SYMBOLS / STOCK_SYMBOLS / COMMODITY_SYMBOLS L433-437
  SUPPORTED_ASSETS L440-446
  get_asset / get_enabled_symbols / validate_symbol / universe_summary L449-476
  ↓ re-export (shim compat)
mercury_ai/config/assets.py (~60 linhas)
  from universe import OPERATIONAL_UNIVERSE, FOREX_UNIVERSE ... L8-22
  FOREX=FOREX_SYMBOLS L28, ASSET_REGISTRY dict L32-60

[FONTE DE RUNTIME — materializada via scripts/sync_broker_registry.py]
data/asset_registry.json (62)  ← AssetRegistry._load_from_file L44
data/brokers/XP.json (62)      ← AssetRegistry.get_assets_for_broker L115
config.json OPERATIONAL_PROFILE {active:"Demo", broker:"XP"} ← MercuryConfigCenter L19

[SCANNER — único ponto de fronteira produção]
mercury_ai/brain/scanner.py:42 __init__
  L5  AssetRegistry → self.asset_registry L63
  L6  MercuryConfigCenter → self.config L65
  L8  MercuryDataProvider → self.provider L45
  L12 MarketDataService(provider) L50
  L4  AnalysisPipeline(market_service) L49
  L10 RankingEngine L58
  L11 InstitutionalBrain L59
  ↓
  scan() L69 → L83 active_profile / L87 active_broker="XP"
            → L89 authorized_symbols=get_assets_for_broker(broker)
            → L104 enabled_assets=[a for a in registry.assets.values()
                 if a.enabled and a.profile==active_profile and a.symbol in authorized_symbols]
            → L125 symbols=[a.symbol for a in enabled_assets]
            → L130 for symbol in symbols: pipeline.analyze(symbol)

[PIPELINE por símbolo] mercury_ai/core/analysis_pipeline.py: analyze(symbol)
  → MarketDataService.get_data(symbol) L105 (data/market_data.py:88)
     → MercuryDataProvider.get_data(symbol) (providers/market_provider.py:140) → YahooAdapter.get_data (data_adapters.py:42, yf.download)
     → DataNormalizer.normalize (data/data_normalizer.py:31)
  → DataQualityEngine.validate(df)
  → IndicatorEngine / MTFEngine / Trend / SmartMoney / Liquidity / Volume / Volatility / Session / MarketState / Evidence / Risk ...
  → brain/mercury_decision_engine.py: MercuryDecisionEngine.analyze
     → ProbabilityEngine → DecisionResolverEngine → DecisionResultBuilder → DecisionResult (audit_id sha256)
  → InstitutionalBrain.explain + snapshot_logger.save

[RANKING] scanner.py:192 min_quality_score=40 filter → 249 ranking_engine.rank(analyses) (analysis/ranking_engine.py:12 sorted by score desc)
         → 312 brain.explain + return ranked L259

[ENTRY] mercury_ai/main.py:1 MercuryScanner().scan() sem args
        operations/m5_operational/runner.py:28 _analyze_one_isolated(symbol) — caller paralelo alternativo
```

**Mapa de referências `grep` em `mercury_ai/**/*.py` (97 hits):**

| Padrão | Resultado |
|--------|-----------|
| `AssetRegistry` | `core/asset_registry.py:31` definição, `brain/scanner.py:5,63` único uso prod |
| `get_assets_for_broker` | `core/asset_registry.py:115`, `brain/scanner.py:91` |
| `filter_assets` / `search_assets` / `get_enabled_assets` | definidos L111-134, **não usados** pelo scanner |
| `watchlist` / `instruments` | **0 hits — NÃO COMPROVADO** |
| `symbols` / `ALL_SYMBOLS` | `universe.py:433` definição, `data_adapters.py:54`, `m5_operational/runner.py:26`, `scanner.py:125` local |

**Fluxo suposto no enunciado** `Fonte→Registry→Scanner→AnalysisPipeline→MarketData→Analysis→Ranking→Decision` está **correto**, mas com nuances: `MarketData` é consumido **dentro** de `AnalysisPipeline` (não depois do Scanner isoladamente), e `MarketDataService` delega a `MercuryDataProvider` antes do `YahooAdapter`.

---

## 6. MERCURY SCANNER AUDIT

**Arquivo:** `mercury_ai/brain/scanner.py` — 407 linhas

| Pergunta | Resposta com prova |
|----------|--------------------|
| Como obtém ativos? | `L63 self.asset_registry=AssetRegistry()` + `L65 self.config=MercuryConfigCenter("config.json")` → `L83 active_profile=config.get("OPERATIONAL_PROFILE","active","Demo")` → `L87 active_broker=config.get(...,"broker","XP")` → `L91 authorized_symbols=asset_registry.get_assets_for_broker(broker)` → `L104-110 enabled_assets` filtrado por `enabled && profile==active && symbol in authorized_symbols` → `L125 symbols=[a.symbol for a in enabled_assets]` |
| Processa todos? | **SIM** — `L130 for symbol in symbols: analysis=pipeline.analyze(symbol)` sem break/sample |
| Filtro anterior? | Somente `config.json` (`active`/`broker`) e `enabled`/`profile`/`authorized_symbols` acima. Sem whitelist adicional |
| Filtro por broker? | **SIM** — `get_assets_for_broker` L91 é o gate. Só `data/brokers/XP.json` existe (62); sem `Clear/Binance.json` |
| Filtro por mercado? | **NÃO** — `scan()` não tem param `market`; `filter_assets(category)` L130 existe mas **nunca chamado** pelo scanner. Mercado é indireto via composição do `XP.json` |
| Aceita lista externa? | **NÃO** — `def scan(self):` L69 **zero** params. `grep -n "def scan"` confirma única definição. `scan(["BTC-USD"])` → `TypeError` |
| Reduzir universo sem alterar pipeline? | **SIM** — via `data/brokers/XP.json` (62→N) ou `data/asset_registry.json` `enabled=false`/`set_enabled` L92 ou `config.json` `broker/active`. Pipeline (`AnalysisPipeline.analyze(symbol)`) é agnóstico — recebe `symbol` livre |

Sort não-determinístico: `L104-123` ordena por `(-favorite,-last_operated,-previous_score,-priority,-liquidity,spread)` onde `last_operated=time.time()` L107 de `asset_registry.py` → ordem depende de wall-clock entre execuções (não afeta ranking de decisão, mas afeta telemetria).

Constantes `PIPELINE_ERROR_STATES` L27 e `PIPELINE_SKIP_STATES` L32 distinguem `DATA_PROVIDER_UNAVAILABLE/PIPELINE_ERROR` (exclui do ranking mas preserva audit) vs `DATA_QUALITY_FAIL/INSUFFICIENT_DATA/MARKET_CLOSED` (SKIP, B2 correction).

---

## 7. MARKET DATA AUDIT

**Inventário real vs doc stale:**

| Requisitado | Real | Veredito |
|-------------|------|----------|
| `data/market_data_service.py` | NÃO existe; canônico é `mercury_ai/data/market_data.py` | Doc stale |
| `providers/` | `providers/market_provider.py`, `data_adapters.py`, `yahoo_finance_provider.py`, `base_provider.py` | VIVO |
| `data/data_adapters/` | NÃO existe; adapters em `providers/data_adapters.py` | Doc stale |
| `core/market_provider.py` | NÃO existe; canônico `providers/market_provider.py` | Doc stale |

**Quem solicita dados (cadeia):**
- `operations/m5_operational/runner.py:38` `MercuryDataProvider()` → `data/market_data.py:88` `MarketDataService.get_data(symbol, interval="5m", period="5d")` → `providers/market_provider.py:140` `MercuryDataProvider.get_data(symbol)` → `providers/data_adapters.py:42` `YahooAdapter.get_data → yf.download(symbol)` → `data/data_normalizer.py:14` `DataNormalizer.normalize(df)` → `data/market_data.py:20` fallback `_normalize_dataframe`.

**Como símbolo é convertido: NÃO É.** `providers/base_provider.py:14 sanitize_symbol` (`_SYMBOL_RE ^[A-Z0-9.\-_]{1,20}$`, rejeita `/`) e `providers/yahoo_finance_provider.py:22` (`^[A-Z0-9.\-_=]{1,20}$` permite `=X/=F` mas rejeita `/`) — `BTC/USD` ou `EUR/USD` com barra → `ValueError/DataValidationError` → `DATA_PROVIDER_UNAVAILABLE`. `data_adapters.py:42` chama `yf.download(symbol)` sem sanitização e só renomeia colunas (`Open→open` L54).

**Forex específico:** formato Yahoo é `{PAR}=X` (`EURUSD=X` `universe.py:62`), `provider_symbol` idêntico a `symbol`. Nenhuma camada insere/remove `=X`.

**Crypto específico:** `{TICKER}-USD` (`BTC-USD` `universe.py:261`), sem `=X`.

**Normalização:** `data/data_normalizer.py:38` lowercases colunas, L52 valida `close` existe, L68 duplica `df["Open"]=df["open"]` para compat engines antigos/novos (refutação case-sensitivity). `data/market_data.py:20` fallback manual `open→Open`. Agnostic a mercado.

**BTC/USD como crypto:** **SIM** — `CRYPTO_UNIVERSE` `BTC-USD` `market="CRYPTO"` L261, `BTC-USD` em `data/asset_registry.json` `category="Cripto"/market="Cripto"` (tradução PT via `scripts/sync_broker_registry.py` `cat_map`).

**Provider aceita 40 desejados?** Yahoo (`supported_assets=ALL_SYMBOLS` L54, `is_implemented=True` L58) aceita os 64 atuais e qualquer subset; 40 desejados são subconjunto se usarem formato Yahoo (`EURUSD=X`/`BTC-USD`). Símbolos com `/` ou sem `=X/-USD` (ex: `BTCUSD`, `EUR/USD`) → **NÃO** (rejeitados por `sanitize_symbol`). Novos `LINK-USD`/`LTC-USD`/`XPL-USD` fora dos 64 precisariam entrar em `CRYPTO_UNIVERSE` e passar por `validate_symbol` L460.

Riscos: `[ALTO]` sem `SymbolNormalizer` (autocorreção `/→-/=X`); `[MÉDIO]` fallback `healthy_providers` em `market_provider.py:40` mascara símbolo inválido tentando Yahoo; `[BAIXO]` `ALL_SYMBOLS` vs `SUPPORTED_ASSETS` L419 drift.

---

## 8. MARKET SESSION / WEEKEND AUDIT

**Inventário de clocks/sessões:**

| Arquivo | Classe | Linhas | Papel |
|---------|--------|--------|-------|
| `sessions/market_sessions.py` | `MarketSessions` | 1-23 | **Único** módulo de sessão — stub |
| `config/sessions.py` | constantes | 1-14 | `SYDNEY/TOKYO/LONDON/NEW_YORK/CLOSED` |
| `operations/m5_operational/clock.py` | `M5Clock` | 1-85 | Scheduler M5 (ceil/floor), não sessão |
| `operations/m5_operational/runner.py` | `M5OperationalRunner` | ~450 | Ciclo operacional, sem gate de sessão |
| `operations/m5_sprint61/live_clock_integrity.py` | `classify_cycle` | 1-310 | Certificação temporal, não calendário |
| `utils/deterministic_clock.py` | `DeterministicClock` | 1-85 | Clock thread-local para replay |
| `calendar/economic_calendar.py` | `EconomicCalendar` | 1-30 | Stub 2 eventos hard-coded |

**`market_sessions.py` integral (23 linhas):**
```python
# mercury_ai/sessions/market_sessions.py:1-23
from datetime import datetime; from mercury_ai.config import sessions
class MarketSessions:
    def get_current_session(self):          # L4
        hour = datetime.utcnow().hour       # L6 naive UTC, sem weekday
        if 22 <= hour or hour < 7: return sessions.SYDNEY
        elif 0 <= hour < 9: return sessions.TOKYO
        elif 7 <= hour < 16: return sessions.LONDON
        elif 13 <= hour < 22: return sessions.NEW_YORK
        return sessions.CLOSED               # L19
    def is_high_liquidity(self):            # L21
        return 13 <= hour < 16
```
`config/sessions.py:1-14` só strings, sem `weekend`/`is_market_open`.

**Busca keywords:** `is_market_open` **0** ocorrências prod; `weekend/Saturday/Sunday/weekday/market_open/market_close/Forex session/Crypto 24/7` **0** em `sessions/`, `m5_operational/`, `calendar/`; `M5OperationalRunner`/`M5Clock` **0** refs a `MarketSessions/weekday/Saturday`.

**Clocks:**
- `M5Clock._next_boundary` L17: `now=datetime.now(timezone.utc); return ceil_m5(now)`; `start()` L24 loop `wait_s=(nxt-now).total_seconds()`, `target=floor_m5(nxt)` L45 → agenda **24/7** sem `weekday` check.
- `M5OperationalRunner._next_target_candle` L140: `floor_m5(now)`; `run_cycle` L180 `target_candle=floor_m5(target or now)` — não filtra `Forex closed`; `FreshnessGate` só valida `decision_candle==latest_candle`.
- `LiveClockIntegrity.classify_cycle` L80: invariantes `target<=clock_now`, `target_close<=decision_ready<next_start`, `latency<300` — distingue `ACCELERATED_SOAK/LIVE_CLOCK` mas **não** `WEEKEND/FOREX_CLOSED`; certifica `PASS` sábado 02:00 para `EURUSD=X`.
- `DeterministicClock` L10 `threading.local()`, L33 `set_time`, L40 `utcnow()` — replay isolation, agnóstico a mercado.

**Perguntas forenses:**
1. Sabe quando Forex fechado? **NÃO** — `get_current_session()` usa só `hour`, retorna `SYDNEY` sábado 03:00 (falso OPEN); `MercuryDataProvider.market_status` L115 fallback `lambda _: "OPEN"`.
2. Sabe quando Crypto pode operar? **NÃO** diferencia — mesmo `ExecutorCls`/`FreshnessGate` para todos (`runner.py:26` `ALL_SYMBOLS`).
3. Diferenciação por asset class? **NÃO** — `MarketSessions` não recebe `symbol/market`, não consulta `get_asset(symbol).market`.
4. Risco de analisar Forex fim de semana? **SIM — EXISTE E É ATIVO.** Todo sábado/domingo 27 pares Forex são escaneados, geram `DATA_PROVIDER_UNAVAILABLE` (Yahoo DF vazio → `MarketClosedException→PIPELINE_ERROR`) ou `WAIT` stale, poluem `runtime_report_*`, distorcem `LiveClock`/`Freshness`.
5. Onde deveria existir? `sessions/market_sessions.py: is_market_open(symbol, dt)` consumindo `universe.get_asset(symbol).market` + regra `FOREX fechado Sat 00:00—Sun 22:00 UTC`, wired em `M5Clock.start()/Runner.run_cycle()` (Gate 1 economia) e `MarketDataService.get_data()` (Gate 2 early `MARKET_CLOSED`) e `FreshnessGate` (Gate 3).

---

## 9. REPLAY IMPACT

| Componente | Arquivo | Classe/método | Linhas | Impacto de reduzir universo |
|------------|---------|---------------|--------|-----------------------------|
| Motor canônico | `analysis/historical_replay_engine.py` | `HistoricalReplayEngine.run_replay(symbol, full_df, n_candles)` | 71-220 | **NENHUM** — `symbol` livre, `provider=HistoricalReplayProvider` L112, `provider.set_data(full_df)` L113, loop `i in range(start_idx, len(full_df)-n_candles)` L140 + `DeterministicClock.set_time` L150 + `provider.set_index(i)` L153 + `cache.get(symbol,i,run_id)` L155, `try/finally restore` L185 (B4-C1), `run_id SHA-256(symbol|n|close)` L120 (B5-C4) |
| Provider | `providers/historical_replay_provider.py` | `HistoricalReplayProvider.get_data/set_data/set_index` | 7-53 | `iloc[:current_index+1]` L23 fatia determinística, sem `AssetRegistry` |
| Batch paralelo | `analysis/replay_batch_processor.py` | `ReplayBatchProcessor.run_batch(data_map:Dict[str,DataFrame])` | 83-175 | `data_map` chaves arbitrárias, sem `XP.json/universe` |
| Cache | `analysis/replay_cache.py` | `ReplayCache.get/put` | 51-83 | key `(symbol,index,run_id)` L44, evita colisão |
| Isolamento | `utils/replay_isolation.py` | `isolated_clock/frozen_clock_at` | 20-48 | sem universo |
| Integridade M5 | `operations/m5_sprint64/replay.py` | `ReplayEngine.replay_cycle` | 28-105 | lê `manifest+cycle_audit/*.json`, checks `ranking_signature/decision_signature`, sem provider |
| Legado | `data/replay_data_provider.py` | `ReplayDataProvider` | 8-25 | `replay_{symbol}.csv` sem registry |

Busca `AssetRegistry|get_assets_for_broker|XP\.json|broker` em 5 arquivos de replay → **0 hits**.

> **Veredito Fase 7:** reduzir universo (62→40 ou 62→2) **altera só candidatos** do Scanner (quantidade de `symbols`, `total_symbols`, `wall_time` linear em `len(data_map)` se caller passar universo reduzido). **Não altera** determinismo, anti look-ahead (`iloc 0..i`), P&L/MAE/MFE, isolamento de clock, fingerprinting, cache.

Testes replay são herméticos: `test_historical_replay_engine.py` `sample_df` sintético `date_range 2024-01-01 100x5min` + symbols `TEST/TINY/SYM-A/CLK-A..E`; `test_replay_batch_processor.py` `{"BTC-USD","ETH-USD","SOL-USD": df sintético}`; `test_replay_cache.py` chaves `BTC-USD/ETH-USD`. Nenhum lê `XP.json` nem assert `len==62`.

---

## 10. TEST IMPACT

Varredura: **115** arquivos (`tests/` 78 + `analysis/tests` 20 + `brain/tests` 4 + `database/tests` 2 + v1_closure 2). `BTC-USD|ETH-USD` → 64 hits em 17 arquivos; `universe_size|== 62|len.*==62` → **0** asserts de contagem fixa.

### TESTES NÃO AFETADOS (~103/115, 89.5%)

Mocks/sintéticos, sem `XP.json/universe`:
`test_historical_replay_engine.py` (sample_df L25), `test_replay_cache.py` (put `BTC-USD,100`), `test_replay_batch_processor.py` (dict explícito L24), `test_replay_identity_pairing.py` (pairing `replay_id` L26), `test_benchmark_framework.py` (`_make_synthetic_ohlcv` L18, `symbols=["BTC-USD"]` L69), `test_asset_registry.py` (`tmp_path/asset_registry.json` L11), `test_confidence_calibration.py`, `test_institutional_backtest.py` (`ASSET_{i}` L49), `test_m5_sprint64_replay.py` (`EURUSD=X` fake L14), `performance/risk/conflict/probability` engines puros, etc.

### TESTES AFETADOS (2 + auditors/scripts) — comportamento muda, mas não quebram hoje

| Arquivo | Linhas | Evidência | Efeito 62→40 |
|---------|--------|-----------|--------------|
| `tests/test_broker_filtering.py` | 15,25,46 | `register_asset("BTC-USD")` L15; `json.dump(["BTC-USD","AAPL","EURUSD"], open("data/brokers/XP.json","w"))` L25; `assert "GOLD" not in symbols` L46 | Sobrescreve `XP.json` **real** em disco e `unlink` L49 — com universo 40 continuaria passando (asserções relativas), mas `tearDown` deletaria novo universo 40 → flakiness CI. Recomendação: usar `tmp_path/data/brokers` |
| `tests/test_scanner_recovery.py` | 12-22 | `register_asset("BTC-USD")` L12, `register_asset("ETH-USD")` L13; `get_assets_for_broker=MagicMock(return_value=["BTC-USD","ETH-USD"])` L16 | Mock fixa 2 autorizados — passa com universo reduzido, mas esconde divergência real |
| `tools/mercury_integrity_auditor/auditors/universe_auditor.py`, `scripts/sync_broker_registry.py`, `scripts/gate_final_asset_universe.py` | — | comparam `XP.json` vs `universe.py` | Auditors passariam a reportar MATCH após redução (hoje 63 PASS/5 UNSUPPORTED) — ajuste esperado |

### TESTES QUE PRECISARIAM ATUALIZAÇÃO FUTURA (6 pontos cosméticos)

| Arquivo | Linhas | Motivo |
|---------|--------|--------|
| `analysis/tests/test_replay_batch_processor.py` | 24,33,169 | fixture 3 símbolos — comentário diverge de doc 40 |
| `analysis/tests/test_benchmark_framework.py` | 150 | batch 2-assets → Sharpe/Sortino com n=2 fraco, validar estabilidade estat. |
| `tests/test_broker_filtering.py` | 25 | hard-coded `["BTC-USD","AAPL","EURUSD"]` inclui `AAPL` fora universo 62 — fixture fictício confunde paridade |
| `docs/REPLAY.md`, `AUDIT_V1/*` | menções `BTC-USD, ETH-USD` top2 | docs citam “Assets analyzed: BTC-USD, ETH-USD” → refletir novo universo |
| `validate_universe_parity.py`, `scripts/asset_universe_*.py` | — | recalibrar thresholds `PerAssetStale/Fresh` se n cair |

Nenhum teste de contagem fixa hoje — recomendação: adicionar `test_universe_parity.py` SSOT `len(XP)==len(FOREX+CRYPTO+STOCKS+COMMODITIES)`.

---

## 11. PERFORMANCE IMPACT

**Sem benchmark de produção rodado (conforme regra do sprint — não inventar %).**

| Métrica | Evidência | Impacto estimado |
|---------|-----------|------------------|
| Quantos entram no scanner hoje? | `data/asset_registry.json` 62 ∩ `XP.json` 62 ∩ `profile==Demo && enabled` → `symbols` L125 = **62** | Base |
| Quantos com universo 40? | 27 FOREX + 10-13 CRIPTO (desired) ou 40 solicitados → **40** (ou 39 contados §12) | `-22` ativos |
| Processamento por ativo | `scanner.py:137 pipeline.analyze(symbol)` → `market_data.get_data` (yfinance 1 chamada) + `DataNormalizer` + `DataQualityEngine` + 15+ engines (`MTFEngine`, `TrendAnalyzer`, `SmartMoney`, `ConfluenceEngine`, `ProbabilityEngine`, `DecisionResolver`, etc L150-250) + `InstitutionalBrain` + `Ranking` | Linear por ativo, sem early skip exceto `DATA_PROVIDER_UNAVAILABLE/MARKET_CLOSED` |
| Paralelo | `M5OperationalRunner` `operations/m5_operational/runner.py` usa `ThreadPoolExecutor` (workers configuráveis) para `ALL_SYMBOLS`; `MercuryScanner.scan()` canônico é **sequencial** `for symbol in symbols` L130 (sem pool) | Ganho paralelo só na variante M5, não no `main.py` canônico |
| Ranking recebe todos? | `scanner.py:249 ranked=ranking_engine.rank(analyses)` recebe só `analyses` não filtrados por `min_quality_score 40` L192 e não `PIPELINE_ERROR/SKIP` | Redução linear em `ranked` |
| Redução diminui carga? | **SIM — linear** em chamadas Yahoo, CPU de engines e wall-clock. Sem número % sem benchmark. | `NÃO QUANTIFICÁVEL COM A EVIDÊNCIA ATUAL` para % de ganho (depende de latência Yahoo, cache TTL 60s, `tracemalloc` ativo, I/O `runtime_report`) |

Cálculo aritmético pontual (não benchmark): `62 → 40 = -22 ativos = -35.5%` de iterações do loop `scan()`.

---

## 12. CURRENT vs DESIRED ASSETS

**Universo desejado do sprint:** 27 FOREX + (12 listados, 13 alegados) CRIPTO = 39-40 ativos.

FOREX desejados (27) — mapeados para Yahoo `=X`:
```
EUR/USD→EURUSD=X, EUR/GBP→EURGBP=X, AUD/JPY→AUDJPY=X, EUR/JPY→EURJPY=X, GBP/USD→GBPUSD=X,
USD/CAD→USDCAD=X, NZD/USD→NZDUSD=X, USD/JPY→USDJPY=X, CAD/JPY→CADJPY=X, CHF/JPY→CHFJPY=X,
EUR/NZD→EURNZD=X, AUD/CHF→AUDCHF=X, EUR/AUD→EURAUD=X, GBP/CHF→GBPCHF=X, GBP/AUD→GBPAUD=X,
USD/AUD→AUDUSD=X*, USD/CHF→USDCHF=X, NZD/JPY→NZDJPY=X, EUR/CHF→EURCHF=X, CAD/CHF→CADCHF=X,
EUR/CAD→EURCAD=X, AUD/NZD→AUDNZD=X, AUD/USD→AUDUSD=X, NZD/CHF→NZDCHF=X, GBP/CAD→GBPCAD=X,
GBP/NZD→GBPNZD=X, NZD/CAD→NZDCAD=X
```
*`USD/AUD` não é par canônico Yahoo — provider aceitaria mas cotação é inversa de `AUDUSD=X` (risco de mapeamento, ver §7 — sem SymbolNormalizer).

CRIPTO desejados (12 nomes do sprint, mapeados):
```
BTC/USD→BTC-USD, Litecoin→LTC-USD, BNB→BNB-USD, XRP→XRP-USD, Ethereum→ETH-USD,
Solana→SOL-USD, AVAX→AVAX-USD, DOGE→DOGE-USD, SUI→SUI-USD, Stellar→XLM-USD,
XPL→XPL-USD, LINK→LINK-USD
```
Contagem sprint diz 13, lista tem 12 — drift de spec.

### Cruzamento com `data/asset_registry.json` (62) e `universe.py` (64)

| Lista | Definição | Prova |
|-------|-----------|-------|
| **MANTER** (atuais que pertencem ao desejado) | 27 FOREX desejados **todos** estão em `universe.py` (28 FOREX) e 26/27 em `registry` (Falta `EURNZD=X` em disco) + 8 CRIPTO desejados estão em `universe`/`registry` (`BTC-USD, BNB-USD, XRP-USD, ETH-USD, SOL-USD, AVAX-USD, DOGE-USD, SUI-USD, XLM-USD` — 9, mas `POL-USD` é extra não desejado) | `FOREX desejados ∩ ALL_SYMBOLS = 27/27`; `CRIPTO desejados ∩ ALL_SYMBOLS = 8-9/12` |
| **REMOVER/DESABILITAR FUTURAMENTE** (atuais fora do desejado) | `registry - desejado = 62 - (27+~9 mantidos) ≈ 26` ativos: 23 STOCKS (`AAPL..WFC`) + 3 COMMODITIES (`CL=F,SI=F,GC=F`) + 1 FOREX não desejado? — na verdade 0 FOREX desejados fora, mas `CADCHF=X` etc são desejados, então todos 27 mantidos; sobram `STOCKS+COMMODITIES` + `POL-USD` | `STOCK_UNIVERSE` 23 + `COMMODITY_UNIVERSE` 3 + `POL-USD` = 27 excedentes se desejado=FOREX+CRIPTO puros |
| **NÃO ENCONTRADO** (desejados sem prova no código) | `LTC-USD` (Litecoin), `LINK-USD`, `XPL-USD` — não estão em `CRYPTO_UNIVERSE` (10) nem em `registry` (9). `USD/AUD` como par direto também não tem `ProviderSymbol` canônico separado (`AUDUSD=X` é o existente) | `LTC-USD in ALL_SYMBOLS → False`, `LINK-USD → False`, `XPL-USD → False` |

Detalhamento `NÃO ENCONTRADO`:
- `LTC-USD` — Litecoin: **NÃO EXISTE** em `CRYPTO_UNIVERSE` (10) nem `registry`; `hezilex_asset_mapping.json` lista `LTC-USD` como `UNSUPPORTED` (5).
- `LINK-USD` — Chainlink: idem `UNSUPPORTED`.
- `XPL-USD` — (Plasma?): idem ausente.
- `USD/AUD` — par `USD/AUD` não é cotação Yahoo direta; canônico é `AUDUSD=X` (inversa). Sem `SymbolNormalizer`, mapear como `USDAUD=X` tentaria Yahoo inexistente → `DATA_PROVIDER_UNAVAILABLE`. Arquiteturalmente deve usar `AUDUSD=X`.

Nenhuma alteração foi feita — listas acima são só diagnóstico.

---

## 13. DUPLICATED ASSET SOURCES

| # | Arquivo | Tipo | Conteúdo | Consumido em prod? | Classificação |
|---|---------|------|----------|--------------------|---------------|
| A | `mercury_ai/config/universe.py` L1-520 | PY fonte | `OPERATIONAL_UNIVERSE` 64 (28+10+23+3), `FOREX_SYMBOLS`/`CRYPTO_SYMBOLS`/`ALL_SYMBOLS` L433, header `L16 FONTE ÚNICA DA VERDADE. NÃO duplique` | **SIM — oficial** | **OFICIAL** |
| B | `mercury_ai/config/assets.py` L1-62 | PY shim | `from universe import OPERATIONAL_UNIVERSE, FOREX_SYMBOLS ...` L8 + aliases `FOREX=FOREX_SYMBOLS` L30, `ASSET_REGISTRY` dict L38 | SIM (compat) | **DERIVADO — não duplicação lógica, mas física** |
| C | `data/asset_registry.json` | JSON cache | 62 entradas PT `category`, gerado por `scripts/sync_broker_registry.py` | SIM (`AssetRegistry._load_from_file` L44, `scanner.py:63`) | **SECUNDÁRIO materializado** (deveria ser sync) |
| D | `data/brokers/XP.json` | JSON cache | 62 símbolos `["AAPL",...]` | SIM (`get_assets_for_broker` L115, `scanner.py:91`) | **SECUNDÁRIO materializado** |
| E | `reports/asset_universe/hezilex_asset_mapping.json` | JSON relatório | 68 entries, 63 PASS (62 distintos), 5 UNSUPPORTED | NÃO — insumo do sync | **Insumo externo** |
| F | `reports/asset_universe/source_of_truth.json` | JSON meta | `asset_count 64` | NÃO | **Metadado auditoria** |
| G | `config.json` | JSON | `OPERATIONAL_PROFILE {active:"Demo",broker:"XP"}` sem lista | NÃO | Sem lista |
| H | `*.yaml/*.env` | — | 0 `*.yaml` com lista (`file_search **/*.yaml` vazio) | NÃO | **Inexistente** |
| I | `app/dashboard/asset_registry_panel.py` L36 | PY display | `asset.category` só para UI | Display | Derivado via registry |

**Veredito:** **UMA FONTE OFICIAL LÓGICA** (`universe.py :: OPERATIONAL_UNIVERSE`) com **2 caches físicos** + **1 shim**. **Não há MÚLTIPLAS fontes independentes** consumidas em prod; risco é **drift** (`universe.py` 64 ≠ disco 62) se `sync_broker_registry.py` não rodar após alterar `universe.py`. Comentários `Total:63`/`27 FOREX` são stale (real 64/28).

---

## 14. FOREX / CRYPTO CLASSIFICATION

| Aspecto | Evidência | Veredito |
|---------|-----------|----------|
| `Asset` dataclass | `core/asset_registry.py:10-28` `category: str` (`Forex`/`Cripto`/`Stocks`/`Commodities` PT), `market: str` duplica `category` (62/62 idênticos) sem `Enum` | Strings livres, `==` case-sensitive |
| `data/asset_registry.json` | `"BTC-USD": {"category":"Cripto","market":"Cripto"}` | **BTC já é Cripto** em disco |
| `universe.py` | `@dataclass UniverseAsset` L35 `market: str # FOREX ou CRYPTO` (EN upper); `FOREX_UNIVERSE` 28 `market="FOREX"` L62; `CRYPTO_UNIVERSE` L194 `BTC-USD` `market="CRYPTO"` L261 | **BTC pertence a `CRYPTO_UNIVERSE` arquiteturalmente — correto** |
| Tradução PT | `scripts/sync_broker_registry.py` `cat_map {"FOREX":"Forex","CRYPTO":"Cripto","STOCK":"Stocks","COMMODITY":"Commodities"}` | `CRYPTO→Cripto` é i18n, não divergência semântica |
| Providers | `data_adapters.py:54` `YahooAdapter supported_assets=ALL_SYMBOLS, supported_markets ["Forex","Stocks","Commodities","Crypto"]` `is_implemented True`; `Polygon/Binance` `CRYPTO_SYMBOLS` stub `is_implemented False` | `BTC-USD` roteado via Yahoo (priority 1 vence) e `CRYPTO_SYMBOLS` para Binance/Polygon |
| Engines | `grep` 97 arquivos `analysis/*.py, brain/*.py` — **0** uso de `category/market` para lógica de sinal | Classificação é metadado de roteamento/display, não afeta fórmula |

**BTC/USD específico:** `BTC-USD` em `CRYPTO_UNIVERSE` (`display_name "BTC/USD"` L261), `provider_symbol "BTC-USD"`, sem `=X`. Código **não** exige `BTC/USD` com `/`; `sanitize_symbol` rejeitaria `/`. Classificação textual atual está **correta**.

---

## 15. ARCHITECTURE IMPACT MAP

### ZONE A — PROTEGIDOS / NÃO TOCAR (inteligência certificada)

| Componente | Arquivo | Classe/método | Evidência de independência |
|------------|---------|---------------|----------------------------|
| MercuryDecisionEngine | `brain/mercury_decision_engine.py` `MercuryDecisionEngine.analyze` | Orquestra Probability→Resolver→Builder; recebe `MarketContext`/`Evidence` já computados, sem `AssetRegistry` | **0 imports** de `AssetRegistry/universe` |
| ProbabilityEngine | `analysis/probability_engine.py` | Fórmula `institutional_strength` + `evidence_bonus`; sem `symbol` | Independente de universo |
| ConfluenceEngine | `analysis/confluence_engine.py` | Score confluência por engine; sem `category` | Idem |
| ConfidenceEngine | `analysis/confidence_engine.py` | Calibração confiança; sem lista de ativos | Idem |
| RankingEngine | `analysis/ranking_engine.py:12 rank()` | `sorted(..., key=score)` — recebe `analyses` já filtrados | Universo-agnóstico (tamanho de lista apenas) |
| TradeFilter | `analysis/trade_filter.py` | Filtro pós-decisão por thresholds | Thresholds fixos, não por mercado |
| DecisionResolver | `analysis/decision_resolver_engine.py` | Regras BUY/SELL/WAIT + `grade/semáforo` | Sem `symbol` list |
| MTFEngine | `analysis/mtf_engine.py` `analyze()` | Consenso multi-timeframe por `symbol` individual | Ignora universo (usa `interval/period` da fatia, degenerado em replay mas não por lista) |
| HistoricalReplayProvider | `providers/historical_replay_provider.py` `get_data` L23 | `iloc[:index+1]` anti look-ahead | 0 deps `AssetRegistry` |
| institutional_weights | `analysis/institutional_weights.py` | Pesos por engine | Sem categoria |
| Regras BUY/SELL/WAIT, thresholds, pesos, confirmation logic | `decision_resolver_engine.py` + `probability_engine.py` | Regras 1-4 resolver, thresholds `institutional_strength` | Protegidos |

*Todos acima classificados **D — SEM IMPACTO** se universo mudar.*

### ZONE B — IMPACTO DIRETO (controlam universo)

| Componente | Arquivo | Classe/método | Tipo de impacto | Evidência |
|------------|---------|---------------|-----------------|-----------|
| Universe SSOT | `config/universe.py` | `OPERATIONAL_UNIVERSE`, `ALL_SYMBOLS`, `get_asset(validate_symbol)` | **Direto — define 64** | `get_enabled_symbols(market)` L449, `validate_symbol` L460 órfã |
| Broker registry | `data/brokers/XP.json` | lista plana 62 | **Direto — gate do scanner** | `scanner.py:91 authorized_symbols=get_assets_for_broker("XP")` |
| Asset registry cache | `data/asset_registry.json` | dict 62 `Asset` | **Direto — interseção** | `scanner.py:104 enabled_assets` ∩ `authorized_symbols` |
| AssetRegistry | `core/asset_registry.py` | `AssetRegistry.get_assets_for_broker` L115, `set_enabled` L92 | **Direto — única API que lê XP.json** | Sem lock, path relativo |
| MercuryScanner | `brain/scanner.py` | `MercuryScanner.scan` L69, `__init__` L42 | **Direto — fronteira** | Sem param `symbols`, monta `symbols` L125 |
| MercuryConfigCenter | `config/configuration_center.py` | `get("OPERATIONAL_PROFILE","broker")` | **Direto — seleciona XP** | `config.json` `broker:"XP"` |
| Sync script | `scripts/sync_broker_registry.py` | `distinct_supported = set(e["internal_symbol"] for e in PASS)` | **Direto — gerador do drift 64→62** | Lê `hezilex_asset_mapping.json` 68 entries |
| Compat shim | `config/assets.py` | `ASSET_REGISTRY` L32 | **Direto — duplicata** | Re-exporta `universe.py` |

### ZONE C — IMPACTO INDIRETO (podem ser afetados)

| Componente | Arquivo | Classe/método | Tipo de impacto | Evidência |
|------------|---------|---------------|-----------------|-----------|
| MarketDataService | `data/market_data.py` `get_data` L88 | 1 chamada Yahoo por `symbol` | **Indireto — carga linear** | Redução 62→40 = -35% chamadas |
| MercuryDataProvider | `providers/market_provider.py` `_get_best_provider` L31 | `min(priority)` sempre Yahoo | **Indireto — sem diferenciação mercado** | `check_health()==is_implemented` só Yahoo passa |
| DataNormalizer | `data/data_normalizer.py` `normalize` L14 | lowercase + duplica `Open/open` | Indireto — volume de normalizações |
| MarketSessions | `sessions/market_sessions.py` `get_current_session` L4 | stub sem `is_market_open` | **Indireto — crítico weekend** | Sem `symbol/dt`, retorna SYDNEY sábado |
| M5Clock | `operations/m5_operational/clock.py` `_next_boundary` L17, `start` L24 | agenda 24/7 | **Indireto — escaneia Forex fim de semana** | Sem `weekday` check |
| M5OperationalRunner | `operations/m5_operational/runner.py` `run_cycle` L180 | `Executor` paralelo sobre `ALL_SYMBOLS` | **Indireto — workers desperdiçados** | Sem `is_market_open` filter |
| LiveClockIntegrity | `operations/m5_sprint61/live_clock_integrity.py` `classify_cycle` L80 | certifica `PASS` mesmo sábado | **Indireto — métrica contaminada** | Não distingue `FOREX_CLOSED` |
| ReplayBatchProcessor | `analysis/replay_batch_processor.py` `run_batch` L125 | `total_symbols=len(data_map)` | Indireto — universo menor = `wall_time` menor |
| Paridade auditors | `tools/mercury_integrity_auditor/auditors/universe_auditor.py` | compara `XP.json` vs `universe.py` | Indireto — report muda após redução |
| Dashboards | `app/dashboard/asset_registry_panel.py` L36 | `asset.category` display | Indireto — lista exibida encolhe |
| `validate_universe_parity.py` | raiz | `for s in ALL_SYMBOLS: yf.Ticker(s).history` | Indireto — valida paridade |

### ZONE D — SEM IMPACTO (independentes comprovados)

`ProbabilityEngine`, `ConfluenceEngine`, `ConfidenceEngine`, `DecisionResolverEngine`, `MTFEngine` (lógica de consenso), `HistoricalReplayEngine/Provider/Cache` (isolados), `InstitutionalMemoryEngine`, `EvidenceQualityEngine`, `BenchmarkFramework` (órfão, usa Yahoo mas é batch sintético), `DeterministicClock`, `EconomicCalendar` (stub), `analysis/*` engines individuais (trend, volume, liquidity, smart_money). Todos sem `AssetRegistry`.

---

## 16. PROTECTED COMPONENTS

Conforme **REGRA ZERO**, os seguintes componentes **NÃO foram auditados intrusivamente** além de provar independência de lista de ativos, e **NÃO devem ser tocados** na implementação futura:

```
MercuryDecisionEngine        brain/mercury_decision_engine.py
ProbabilityEngine            analysis/probability_engine.py
ConfluenceEngine             analysis/confluence_engine.py
ConfidenceEngine             analysis/confidence_engine.py
RankingEngine                analysis/ranking_engine.py
TradeFilter                  analysis/trade_filter.py
DecisionResolver             analysis/decision_resolver_engine.py
MTFEngine                    analysis/mtf_engine.py
HistoricalReplayProvider     providers/historical_replay_provider.py
institutional_weights        analysis/institutional_weights.py
regras BUY / SELL / WAIT     decision_resolver_engine.py
thresholds / pesos / scoring / confirmation logic / institutional intelligence
```

Prova de proteção: nenhum dos arquivos acima importa `AssetRegistry`, `XP.json`, `universe.ALL_SYMBOLS` para lógica de decisão (imports verificados via `grep` + leitura direta). Alterar universo **não requer** recalibrar pesos.

---

## 17. FUTURE ARCHITECTURE PROPOSAL

**Objetivo:** reduzir 62→40 (27 FOREX 24/5 + 12-13 CRIPTO 24/7) reutilizando componente existente, sem criar módulo novo.

**Proposta mínima (ordem de prioridade):**

### 1) Reutilizar componente existente — `AssetRegistry` + `data/brokers/XP.json` (1 gate + 1 filtro)

**Zero código novo obrigatório para reduzir contagem.** Basta:
- Editar `data/brokers/XP.json`: manter só 27 FOREX `=X` desejados + 12-13 CRIPTO `-USD` desejados (total 39-40).
- Em `data/asset_registry.json`: `set_enabled(symbol, False)` para 22 excedentes (STOCKS+COMMODITIES+`POL-USD`/`EURNZD=X` órfãos) via `AssetRegistry.set_enabled` L92 (preserva `last_operated/previous_score` no JSON atômico).
- Resultado: `scanner.py:104` `enabled && profile==Demo && symbol in authorized_symbols` naturalmente restringe a 40 sem tocar `scanner.py`.

### 2) Alterar configuração existente — `mercury_ai/config/universe.py` (SSOT alignment)

Se desejado for **40 canônico** (não 64), alinhar SSOT:
- Em `universe.py`: remover `STOCK_UNIVERSE` 23 + `COMMODITY_UNIVERSE` 3 do `OPERATIONAL_UNIVERSE` (ou `enabled=False` por `UniverseAsset`), adicionar `LTC-USD/LINK-USD/XPL-USD` a `CRYPTO_UNIVERSE`.
- Rodar `scripts/sync_broker_registry.py` (sem filtro Hezilex `distinct PASS` — usar `ALL_SYMBOLS` direto) para regenerar `data/brokers/XP.json` + `data/asset_registry.json` coerentes (corrige drift 64 vs 62).
- Eliminar `mercury_ai/config/assets.py::ASSET_REGISTRY` L32 ou marcar `@deprecated` + teste de paridade `len(ALL_SYMBOLS)==len(registry)==len(XP)`.

### 3) Calendário — `mercury_ai/sessions/market_sessions.py` (único módulo novo lógico, mas alteração de existente)

Expandir `market_sessions.py` de 23 linhas stub para:
```python
def is_market_open(self, symbol: str, dt: datetime) -> bool:
    asset = get_asset(symbol)  # from mercury_ai.config.universe
    if asset.market == "CRYPTO": return True  # 24/7
    if asset.market == "FOREX":  return not is_forex_weekend(dt)  # Sat 00:00 — Sun 22:00 UTC
    if asset.market == "STOCK":  return is_nyse_open(dt)  # se stocks permanecerem, 13:30-20:00 UTC
    return True
```
Wiring: `operations/m5_operational/runner.py:run_cycle()` filtrar `symbols=[s for s in universe if is_market_open(s, target_candle)]` antes de `executor.submit` (Gate 1 economia), `data/market_data.py:get_data()` early `raise MarketClosedException` se `not is_market_open` (Gate 2).

### 4) Criar módulo novo — **NÃO NECESSÁRIO.** Tudo cabe em (1)+(2)+(3). Só criar `mercury_ai/sessions/symbol_normalizer.py` se quiser `SymbolNormalizer` (`BTC/USD→BTC-USD`, `USD/AUD→AUDUSD=X`) — caso contrário, documentar que `/` é inválido.

**Não implementar neste sprint** — apenas proposta.

---

## 18. FUTURE IMPLEMENTATION SCOPE

| Fase futura | Escopo | Estimativa |
|-------------|--------|------------|
| **F1 — Redução de universo (sem inteligência)** | Editar `data/brokers/XP.json` (62→40) + `data/asset_registry.json` `enabled=false` 22 excedentes. Validar `python -c len(json.load(open('data/brokers/XP.json')))==40` + `main.py` smoke 1 ciclo. | 1 arquivo JSON + `registry.save()` — 0 linhas de lógica |
| **F2 — Alinhamento SSOT** | `mercury_ai/config/universe.py` `OPERATIONAL_UNIVERSE` 64→40 + `scripts/sync_broker_registry.py` sem filtro Hezilex + `validate_universe_parity.py` | ~20 linhas em `universe.py` + 1 script |
| **F3 — Calendário Forex 24/5 / Crypto 24/7** | `sessions/market_sessions.py` `is_market_open(symbol,dt)` + `runner.py` Gate 1 + `market_data.py` Gate 2 + `live_clock_integrity.py` novo estado `MARKET_CLOSED` | ~60 linhas em 3 arquivos, sem tocar engines |
| **F4 — Higiene** | Deprecar `config/assets.py::ASSET_REGISTRY`, adicionar `tests/test_universe_parity.py`, isolar `test_broker_filtering.py` para `tmp_path`, documentar `SymbolNormalizer` | ~40 linhas testes/docs |
| **Fora de escopo** | Qualquer alteração em Zone A (`ProbabilityEngine`, `DecisionResolver`, pesos, thresholds, regras BUY/SELL/WAIT) | **PROIBIDO** |

Total futuro: **~4 arquivos tocados, <100 linhas lógicas**, 0 motores.

---

## 19. FILES AUTHORIZED FOR FUTURE IMPLEMENTATION

```
mercury_ai/config/universe.py                     # SSOT — remover STOCKS/COMMODITIES, add LTC/LINK/XPL se desejado
data/brokers/XP.json                              # broker whitelist 62→40 (ou broker novo FOREX.json/CRYPTO.json)
data/asset_registry.json                          # enabled=false para excedentes (via AssetRegistry.set_enabled L92)
mercury_ai/sessions/market_sessions.py            # is_market_open(symbol, dt) + is_forex_weekend()
operations/m5_operational/runner.py               # Gate 1: filtrar symbols por is_market_open antes do Executor
mercury_ai/data/market_data.py                    # Gate 2: early MARKET_CLOSED sem yfinance fora de horário
scripts/sync_broker_registry.py                   # remover filtro Hezilex distinct PASS, usar ALL_SYMBOLS
validate_universe_parity.py                       # ajustar validação 64→40
mercury_ai/config/assets.py                       # deprecar ASSET_REGISTRY (opcional)
tests/test_broker_filtering.py                    # isolar para tmp_path (higiene)
tests/test_universe_parity.py                     # NOVO — teste de paridade 40 (higiene)
tools/mercury_integrity_auditor/auditors/universe_auditor.py  # thresholds 62→40
```

---

## 20. FILES FORBIDDEN FROM MODIFICATION

```
mercury_ai/brain/mercury_decision_engine.py      # MercuryDecisionEngine
mercury_ai/analysis/probability_engine.py         # ProbabilityEngine
mercury_ai/analysis/confluence_engine.py          # ConfluenceEngine
mercury_ai/analysis/confidence_engine.py          # ConfidenceEngine
mercury_ai/analysis/ranking_engine.py             # RankingEngine
mercury_ai/analysis/trade_filter.py               # TradeFilter
mercury_ai/analysis/decision_resolver_engine.py   # DecisionResolver / regras BUY/SELL/WAIT / grade / semáforo
mercury_ai/analysis/mtf_engine.py                 # MTFEngine (consenso multi-timeframe)
mercury_ai/providers/historical_replay_provider.py # HistoricalReplayProvider
mercury_ai/analysis/institutional_weights.py      # institutional_weights
mercury_ai/analysis/decision_explainability.py    # dataclass TradingExplanation
mercury_ai/core/analysis_pipeline.py              # AnalysisPipeline (pipeline de decisão)
 TODOS os thresholds / pesos / probabilidades / scoring / confirmation logic / institutional intelligence
```

Qualquer PR que toque os arquivos acima sob pretexto de “reduzir universo” deve ser **bloqueado**.

---

## 21. REGRESSION REQUIREMENTS

Antes de merge da implementação futura (F1-F3), exigir:

1. **`git status --short` limpo** exceto `data/brokers/XP.json` + `data/asset_registry.json` + `mercury_ai/sessions/market_sessions.py` + `operations/m5_operational/runner.py` (whitelist §19).
2. **`python -m py_compile`** em `mercury_ai/config/universe.py`, `mercury_ai/sessions/market_sessions.py`, `operations/m5_operational/runner.py` — 0 `SyntaxError`.
3. **`pytest` subset:** `tests/test_broker_filtering.py` (após isolar tmp_path), `tests/test_scanner_recovery.py`, `mercury_ai/analysis/tests/test_historical_replay_engine.py` (21/21 clock/replay), `mercury_ai/analysis/tests/test_replay_batch_processor.py`, `tests/test_m5_sprint64_replay.py` — sem novos `FAILED` além dos 4 pré-existentes documentados (`test_auto_health`, `test_confidence_calibration_auditor`, `test_institutional_report_generator`, `test_robustness`).
4. **Smoke `main.py` 1 ciclo:** `python -m mercury_ai.main` (ou `scripts/run_scanner_once.py`) deve logar `Assets authorized: 40` (ou 39) e `Ranked: 40` com `decision_candle` coerente; `BTC-USD` deve aparecer como `Cripto/CRYPTO` no `runtime_report`.
5. **Paridade:** `python -c "from mercury_ai.config.universe import ALL_SYMBOLS; import json; assert len(ALL_SYMBOLS)==len(json.load(open('data/brokers/XP.json')))==len(json.load(open('data/asset_registry.json')))"`.
6. **Replay determinism:** `python -m pytest mercury_ai/analysis/tests/test_historical_replay_engine.py::TestDeterministicClockIsolation -q` — 2 replays P/L idênticos.
7. **Weekend gate (após F3):** simular `target_candle=Saturday 14:00 UTC`: `is_market_open("EURUSD=X", dt) is False`, `is_market_open("BTC-USD", dt) is True`; `runner.run_cycle` deve pular 27 FOREX e manter 9-13 CRIPTO.
8. **Snapshots:** após `pytest` full, restaurar `git checkout -- mercury_ai/database/snapshots/` (174 deletados — side-effect R1 conhecido).

---

## 22. FINAL VERDICT

### AUDIT COMPLETE — IMPLEMENTATION NOT YET AUTHORIZED

**Justificativa exclusiva na evidência:**

- Arquitetura **suporta** universo operacional restrito de 40 **sem** alterar inteligência: `AssetRegistry` já filtra por `enabled/profile/authorized_symbols` (`core/asset_registry.py:92,104,115` + `brain/scanner.py:91,104`), `universe.py` já expõe `FOREX_SYMBOLS/CRYPTO_SYMBOLS/ALL_SYMBOLS` (`L433-437`) e `get_enabled_symbols(market)` (`L449`).
- **Nenhum bloqueador arquitetural** foi encontrado: `scan()` sequencial aceita universo menor via `XP.json`/`enabled`; replay é agnóstico; providers Yahoo aceitam todos os 40 no formato `=X/-USD` (prova §7).
- **Riscos mapeados são não-bloqueantes:** drift 64≠62 (`EURNZD=X/POL-USD`), ausência de `is_market_open`/weekend gate, e 2 fixtures que tocam `XP.json` real. Todos corrigíveis em <100 linhas (§17) sem Zone A.
- **Não há motivo para `BLOCKED`.** Também não é `READY FOR IMPLEMENTATION` porque a implementação **não foi feita nem autorizada neste sprint** (regra `CRITICAL STOP CONDITION`).

O universo de 40 proposto (`FOREX 27 Monday-Friday` + `CRYPTO 13 24/7`) é **representável** pela arquitetura atual se `sessions/market_sessions.py` ganhar `is_market_open(symbol, dt)` (§17.3) — hoje seria 39 efetivos no Yahoo (`LTC-USD/LINK-USD/XPL-USD` ausentes do código + `USD/AUD` inverso).

**Próximo passo autorizado:** aguardar `GO` do operador para executar F1 (mutação de `XP.json`/`asset_registry.json` para 40) — sem tocar F2/F3 até aprovação.

---

## GIT / INTEGRIDADE — pós-auditoria

Pré-auditoria (capturado 2026-09-08):
```
git status --short  → (limpo)
git diff --stat     → (vazio)
git diff --cached --stat → (vazio)
git branch --show-current → main
git log --oneline -5 → e642b5d Merge main / 11ca4e89 / 6593e76c / 7bff056e / be3aa884
```

Pós-auditoria (único artefato criado — sem `git add`):
```
git status --short
?? S7.X_OPERATIONAL_ASSET_UNIVERSE_IMPACT_AUDIT.md
```
Nenhum arquivo de produção, teste ou configuração foi modificado (validado via `git diff --stat` vazio).

---

## STOP CONDITION — respeitada

```
NÃO implementado novo universo
NÃO alteradas listas de ativos
NÃO alterado AssetRegistry
NÃO alterado Scanner
NÃO alterado Market Data
NÃO alterado calendário
NÃO alterado replay
NÃO alterados testes
NÃO alterada inteligência
```

Apenas este relatório foi entregue.

---

## ANEXO — Evidência por arquivo/linha (índice forense)

| Fase | Arquivo | Classe/método | Linhas |
|------|---------|---------------|--------|
| F1 | `mercury_ai/config/universe.py` | `OPERATIONAL_UNIVERSE/ALL_SYMBOLS` | 1-476 |
| F1 | `data/asset_registry.json` | 62 entries | — |
| F1 | `data/brokers/XP.json` | lista 62 | — |
| F1 | `reports/asset_universe/hezilex_asset_mapping.json` | 68 entries | — |
| F1 | `reports/asset_universe/source_of_truth.json` | `SOURCE_OF_TRUTH` | — |
| F2 | `mercury_ai/core/asset_registry.py` | `Asset`/`AssetRegistry` | 10-134 |
| F2 | `mercury_ai/core/asset_registry.py` | `get_assets_for_broker` | 115-123 |
| F2 | `mercury_ai/core/asset_registry.py` | `get_enabled_assets/filter_assets` | 111-134 |
| F3 | `mercury_ai/brain/scanner.py` | `MercuryScanner.scan` | 69-259 |
| F4 | `mercury_ai/brain/scanner.py` | `scan() sem param` | 69 |
| F5 | `mercury_ai/data/market_data.py` | `MarketDataService.get_data` | 88-147 |
| F5 | `mercury_ai/providers/market_provider.py` | `MercuryDataProvider.get_data` | 140-148 |
| F5 | `mercury_ai/providers/data_adapters.py` | `YahooAdapter.get_data` | 42-77 |
| F5 | `mercury_ai/data/data_normalizer.py` | `DataNormalizer.normalize` | 14-73 |
| F5 | `mercury_ai/providers/base_provider.py` | `sanitize_symbol` | 4-33 |
| F5 | `mercury_ai/providers/yahoo_finance_provider.py` | `_sanitize_symbol` | 22-54 |
| F6 | `mercury_ai/sessions/market_sessions.py` | `MarketSessions` | 1-23 |
| F6 | `mercury_ai/operations/m5_operational/clock.py` | `M5Clock` | 1-85 |
| F6 | `operations/m5_operational/runner.py` | `M5OperationalRunner` | 28,140,180 |
| F7 | `mercury_ai/analysis/historical_replay_engine.py` | `HistoricalReplayEngine.run_replay` | 71-220 |
| F7 | `providers/historical_replay_provider.py` | `HistoricalReplayProvider` | 7-53 |
| F7 | `analysis/replay_batch_processor.py` | `ReplayBatchProcessor.run_batch` | 83-175 |
| F7 | `analysis/replay_cache.py` | `ReplayCache` | 51-83 |
| F8 | `tests/test_broker_filtering.py` | — | 15,25,46 |
| F8 | `tests/test_scanner_recovery.py` | — | 12-22 |
| F11 | `mercury_ai/config/assets.py` | `ASSET_REGISTRY` | 1-62 |
| F12 | `mercury_ai/config/universe.py` | `CRYPTO_UNIVERSE BTC-USD` | 194-261 |
| F12 | `data/asset_registry.json` | `BTC-USD category Cripto` | — |
| F12 | `tools/mercury_integrity_auditor/auditors/universe_auditor.py` | `UniverseAuditor` | — |
| — | `config.json` | `OPERATIONAL_PROFILE` | — |
| — | `mercury_ai/main.py` | `main()` | 1-10 |
| — | `conftest.py` | `pytest_configure` | — |

---

## FIM DO SPRINT DE AUDITORIA

