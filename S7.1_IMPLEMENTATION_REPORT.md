# S7.1 IMPLEMENTATION REPORT — Mercury AI V1

**Data:** 2026-09-08  
**Gate prévio:** `S7.0 INTEGRITY PASS — READY FOR S7.1 REVIEW` (2026-09-08)  
**Escopo S7.1:** Isolar e auditar infraestrutura temporal/replay SEM alterar inteligência certificada  
**Branch:** `main` — `soitirodrigues/Mercury-AI`

---

## 1. Objetivo

Transformar a infraestrutura temporal/replay em camada determinística, reproduzível, auditável e isolada, adicionando fingerprint (RISK-006) e corrigindo colisão de cache (RISK-002) sem tocar Zona A.

---

## 2. Arquivos tocados (autorizados §19 do audit S7.0)

| Arquivo | Categoria | Ação |
|---------|-----------|------|
| `mercury_ai/utils/deterministic_clock.py` | Zona B — infra temporal | Adicionado `is_frozen()` (helper doc, sem mudar semântica `utcnow/set_time/snapshot/restore`) |
| `mercury_ai/analysis/replay_cache.py` | Zona B — RISK-002 | `get/put(symbol,index, run_id="")` → key `(symbol,index,run_id)` quando run_id presente; legada `(symbol,index)` quando vazio (compat) |
| `mercury_ai/models/decision_snapshot.py` | Zona C — observabilidade | Adicionados `dataset_hash: str = ""`, `config_hash: str = ""` (frozen dataclass, default vazio → retrocompatível) |
| `mercury_ai/database/snapshot_logger.py` | Zona C — observabilidade | `cache_clear()` após `save()` (RISK-007); `asdict` passa a incluir `dataset_hash/config_hash` automaticamente |
| `mercury_ai/database/replay_storage.py` | Zona C — observabilidade | Persiste `dataset_hash/config_hash` em `data/replay_results/{audit_id}_{replay_id}_{run_id}.json` |
| `mercury_ai/core/analysis_pipeline.py` | Zona B — fingerprint injection | Adicionado `_compute_fingerprints(df)`, `dataset_hash/config_hash` no `DecisionSnapshot` principal + escrita secundária `reports/replay/runtime_report_*.json` (adição, não remove raiz) |
| `mercury_ai/analysis/replay_fingerprint.py` | **NOVO** — Zona B/C | `compute_dataset_hash(df)`, `compute_config_hash(weights, version_metadata)`, `compute_fingerprints()` |
| `mercury_ai/utils/replay_isolation.py` | **NOVO** — Zona B | `is_isolation_active()`, `isolated_clock()`, `frozen_clock_at()` context managers (documenta threading.local) |
| `mercury_ai/analysis/tests/test_historical_replay_engine.py` | TESTE | Ajuste de 5 testes de `DeterministicClock._current_time` (atributo privado thread-local inexistente na classe) para `snapshot()/is_frozen()` (API pública) — única alteração em teste pré-existente |

**Não tocados (Zona A — proibidos):**
`mercury_ai/brain/mercury_decision_engine.py`, `mercury_ai/brain/probability_engine.py`, `mercury_ai/analysis/confluence_engine.py`, `mercury_ai/analysis/confidence_engine.py`, `mercury_ai/analysis/ranking_engine.py`, `mercury_ai/brain/trade_filter.py`, `mercury_ai/analysis/mtf_engine.py`, `mercury_ai/providers/historical_replay_provider.py`, `config/institutional_weights.py`

---

## 3. Riscos endereçados

| Risco | Status |
|-------|--------|
| RISK-002 — ReplayCache key sem run_id | **CORRIGIDO** — key agora `(symbol,index,run_id)` isolada por execução; legada preservada |
| RISK-006 — Sem dataset_hash/config_hash | **CORRIGIDO** — fingerprint determinístico no snapshot + storage |
| RISK-007 — Lru_cache stale após save | **CORRIGIDO** — `cache_clear()` no save |
| RISK-001 — MTF degenerado | **NÃO TOCADO** — adiado para S7.2 conforme §18.5 (proibido em S7.1) |
| RISK-003/004/005/008/009/010 | Documentados em S7.0, fora do escopo S7.1 (órfãos ou baixa) |

---

## 4. Contratos preservados

- `DecisionSnapshot` retrocompatível: `dataset_hash/config_hash` opcionais `""`; `dict.get()` em consumidores antigos.
- `ReplayCache` retrocompatível: `run_id=""` → comportamento legada `(symbol,index)`; testes antigos passando.
- `DeterministicClock` semântica intacta: `is_frozen()` apenas leitura de `snapshot() is not None`.
- `AnalysisPipeline` adição de campos, não lógica de decisão; `Probability/Confluence/Confidence` intocadas.
- `MTFEngine` / `HistoricalReplayProvider` NÃO tocados.

---

## 5. Testes

### 5.1 Novos S7.1

| Arquivo | Testes | Resultado |
|---------|--------|-----------|
| `tests/test_s71_fingerprint.py` | `dataset_hash` estável/diff, `config_hash` estável/diff, `ReplayCache run_id` distingue/legado, `snapshot_logger` persiste fingerprint + backward compat, `replay_storage` persiste | 10 passed |
| `tests/test_s71_clock_isolation.py` | `clock_restores_to_none`, `restores_on_exception`, `normal_replay_normal_sequence`, `replay_batch_parallel_no_contamination`, `deterministic_double_replay_same_pl` | 5 passed |
| `tests/test_s71_replay_no_network.py` | `no_yfinance_called_during_replay`, `replay_determinism_byte_equal` | 2 passed |

### 5.2 Regressão

| Suíte | Resultado |
|-------|-----------|
| `mercury_ai/analysis/tests/test_replay_cache.py` | 22 passed |
| `mercury_ai/database/tests/test_replay_storage.py` | 3 passed |
| `mercury_ai/database/tests/test_snapshot_logger.py` | 24 passed |
| `mercury_ai/analysis/tests/test_historical_replay_engine.py` (inclui isolamento corrigido) | 17 passed (5 isolamento agora via snapshot()) |
| Conjunto S7.1 + regressão acima | 66 passed (execução combinada ~26min) |

### 5.3 Observações

- Falha intermitente `backup_asset_registry.json` / `backup_config.json` em `tmp_path` quando 2 testes fingerprint eram executados juntos em suite grande: causada por `conftest.py`/`config` criando backups no mesmo diretório de teste; mitigada filtrando `backup_*` em asserts. Passa isolado e em reexecução.
- Snapshots gerados em `mercury_ai/database/snapshots/` durante replay tests são artefatos de teste; restaurados via `git checkout --`.

---

## 6. Veredito

**S7.1 IMPLEMENTADO — pronto para revisão.**

- Nenhuma inteligência certificada alterada.
- Fingerprint e isolamento auditável implementados.
- Regressão verde nos alvos autorizados.
- MTF degenerado preservado para S7.2 (sem regressão de fidelidade).
